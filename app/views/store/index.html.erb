<% @stores.each do |e| %>
  <p>
    <%= e.name %>
    <%= e.address %>
    <%= e.category_1 %>
    <%= e.category_2 %>
    <%= link_to '削除', store_path(e), method: :delete %>
    </p>
<% end %>

<p>
  <%= link_to '新規店舗追加', new_store_path %>
</p>

<p>
  <%= link_to '店舗一覧', articles_path %>
</p>